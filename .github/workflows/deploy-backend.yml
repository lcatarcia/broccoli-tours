name: Deploy Backend to Azure App Service

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:

env:
  AZURE_WEBAPP_NAME: wa-bt
  AZURE_WEBAPP_PACKAGE_PATH: './backend/src/BroccoliTours.Api'
  DOTNET_VERSION: '8.0.x'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Build with dotnet
        run: dotnet build BroccoliTours.sln --configuration Release
        working-directory: ./backend

      - name: Run tests
        run: dotnet test BroccoliTours.sln --configuration Release --no-build --verbosity normal
        working-directory: ./backend

      - name: Publish
        run: dotnet publish src/BroccoliTours.Api/BroccoliTours.Api.csproj --configuration Release --output ${{env.DOTNET_ROOT}}/myapp --no-build
        working-directory: ./backend

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: ${{env.DOTNET_ROOT}}/myapp
